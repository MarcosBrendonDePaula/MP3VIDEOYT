<ul class="list-group">
    {{#each quee}}
        <li class="list-group-item d-flex justify-content-between align-items-start">
        <div class="ms-2 me-auto">
        <div class="fw-bold">{{videoId}}</div>
        Tempo estimado: {{progress.estimative}} segundos
        </div>
        <span class="badge bg-primary rounded-pill">{{progress.percentage}}</span>
    </li>
    {{/each}}
</ul>